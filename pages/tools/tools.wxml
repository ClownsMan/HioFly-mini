<view class="container">
  <view class="tools-grid">
    <view class="tool-item" wx:for="{{tools}}" wx:key="id" bindtap="navigateToTool" data-url="{{item.url}}">
      <image src="{{item.icon}}"/>
      <text>{{item.name}}</text>
    </view>
  </view>
</view> 